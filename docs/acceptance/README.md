# Local Acceptance Evidence

This folder contains small, isolated artifacts created by Mission Control's local
acceptance runs. Each Queue fixture owns one file so MC Native and OpenCode can be
tested independently without editing the runtime or colliding with another run.

These files are local qualification evidence, not production configuration.
